services:
  - type: web
    name: schedule-ai
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: NODE_VERSION
        value: 22.12.0
      - key: PORT
        value: 5000
